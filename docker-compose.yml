services:

  # nginx:
  #   build: ./frontend
  #   ports:
  #     - "8082:443"
  #   depends_on:
  #     - gateway
  #   networks:
  #     - backend

  frontend-dev:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - gateway
    networks:
      - backend

  gateway:
    build: ./gateway
    ports:
      - "8080:8080"
    env_file:
      - ./gateway/.env
    depends_on:
      - auth-service
    networks:
      - backend
  
  auth-service:
    build: ./auth-service
    expose:
      - "8081"
    networks:
      - backend

  livechat-service:
    build: ./chat-service
    expose:
      - "8083"
    networks:
      - backend

networks:
  backend:
    name: backend
    driver: bridge